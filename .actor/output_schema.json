{
  "actorOutputSchemaVersion": 1,
  "title": "Delivery check report",
  "properties": {
    "report": { "type": "string", "title": "Full report", "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT" },
    "dataset": { "type": "string", "title": "Results", "template": "{{links.apiDefaultDatasetUrl}}/items" }
  }
}
